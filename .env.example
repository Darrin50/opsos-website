# Resend (transactional email) — approved provider, control-plane issue #58.
# Required in production for /api/demo and /api/contact to actually send mail.
# Never commit real values; server-side only, never exposed to the client.
RESEND_API_KEY=
RESEND_FROM_EMAIL=
OPSOS_LEADS_TO=
